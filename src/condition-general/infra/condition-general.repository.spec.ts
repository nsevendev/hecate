import { ConditionGeneralRepository } from './condition-general.repository'

describe('ConditionGeneralRepository', () => {
  it('should be defined', () => {
    expect(new ConditionGeneralRepository()).toBeDefined()
  })
})
